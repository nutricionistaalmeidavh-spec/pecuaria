import assert from 'node:assert/strict';import {createHash} from 'node:crypto';import {createReadStream} from 'node:fs';import {readFile,readdir,rm,stat} from 'node:fs/promises';import {basename,join} from 'node:path';import {fileURLToPath} from 'node:url';import {currentCommit,writeEvidence} from './evidence.mjs';const artifactDir=fileURLToPath(new URL('../qa-artifacts/',import.meta.url)),releaseDir=fileURLToPath(new URL('../release/',import.meta.url)),certPath=join(artifactDir,'release-certification.json'),INSTALLER=/^ArtiSys-Pecuaria-Setup-.*\.exe$/i,PIN='a444031860d5b8c91adc5628759bc67c0c29d557';const json=async p=>JSON.parse(await readFile(p,'utf8'));const hashFile=p=>new Promise((resolve,reject)=>{const h=createHash('sha256'),s=createReadStream(p);s.on('data',c=>h.update(c));s.on('error',reject);s.on('end',()=>resolve(h.digest('hex')));});export function validateEvidence(commit,evidence){for(const [name,item] of Object.entries(evidence)){assert.equal(item.status,'passed',`${name} evidence is not passed`);assert.equal(item.commit,commit,`${name} evidence is stale`);}return true;}export function selectInstaller(c){assert.ok(c.length,'Windows installer not found');const i=[...c].sort((a,b)=>b.mtimeMs-a.mtimeMs)[0];assert.ok(i.size>1024*1024,'Installer must be larger than 1 MiB');return i;}export function validateReleaseRun({head,run,installer}){assert.equal(run.status,'passed','release run is not passed');assert.equal(run.commit,head,'release run is stale');assert.equal(run.utilidadesCommit,PIN,'release run used unexpected utilidades commit');assert.ok(Number.isFinite(Date.parse(run.startedAt)),'release run startedAt is invalid');assert.ok(installer.mtimeMs>=Date.parse(run.startedAt),'installer predates release run');assert.equal(installer.name,run.installer?.name,'installer name differs from release run');assert.equal(installer.sha256,run.installer?.sha256,'installer hash differs from release run');return true;}export async function certifyRelease(){await rm(certPath,{force:true});const commit=await currentCommit(),evidence={phase5:await json(join(artifactDir,'phase5-summary.json')),phase7:await json(join(artifactDir,'phase7-summary.json')),playwright:await json(join(artifactDir,'playwright-summary.json'))};validateEvidence(commit,evidence);const run=await json(join(artifactDir,'release-run.json')),names=(await readdir(releaseDir)).filter(n=>INSTALLER.test(n)),candidates=await Promise.all(names.map(async name=>{const info=await stat(join(releaseDir,name));return{name,path:join(releaseDir,name),size:info.size,mtimeMs:info.mtimeMs,mtime:info.mtime};}));let installer=candidates.find(x=>x.name===run.installer?.name)??selectInstaller(candidates);installer={...installer,sha256:await hashFile(installer.path)};validateReleaseRun({head:commit,run,installer});const record={phase:8,status:'passed',productId:'agro-pecuaria',evidence:{phase5:evidence.phase5.generatedAt,phase7:evidence.phase7.generatedAt,playwright:evidence.playwright.generatedAt,releaseRun:run.finishedAt},installer:{name:installer.name,size:installer.size,mtime:installer.mtime.toISOString(),sha256:installer.sha256},utilidadesCommit:PIN};await writeEvidence(certPath,{...record,commit});return record;}if(basename(process.argv[1]??'').toLowerCase()==='certify-release.mjs')certifyRelease().then(()=>console.log('[PASS] FASE 8 - release certificado')).catch(e=>{console.error(`[FAIL] FASE 8 - ${e.message}`);process.exitCode=1;});
