import test from 'node:test';import assert from 'node:assert/strict';import {validateEvidence,selectInstaller,validateReleaseRun} from '../tooling/certify-release.mjs';const head='a'.repeat(40),pin='a444031860d5b8c91adc5628759bc67c0c29d557';test('certification rejects stale evidence',()=>assert.throws(()=>validateEvidence(head,{phase5:{status:'passed',commit:'b'.repeat(40)},phase7:{status:'passed',commit:head},playwright:{status:'passed',commit:head}}),/stale/));test('certification rejects missing installer',()=>assert.throws(()=>selectInstaller([]),/installer not found/i));test('certification rejects installer from previous release run',()=>assert.throws(()=>validateReleaseRun({head,run:{status:'passed',commit:head,utilidadesCommit:pin,startedAt:'2026-09-18T10:00:00Z',installer:{name:'x.exe',sha256:'x'}},installer:{name:'x.exe',mtimeMs:Date.parse('2026-09-18T09:00:00Z'),sha256:'x'}}),/predates release run/));test('certification rejects changed installer hash',()=>assert.throws(()=>validateReleaseRun({head,run:{status:'passed',commit:head,utilidadesCommit:pin,startedAt:'2026-09-18T10:00:00Z',installer:{name:'x.exe',sha256:'a'}},installer:{name:'x.exe',mtimeMs:Date.parse('2026-09-18T11:00:00Z'),sha256:'b'}}),/hash differs/));
