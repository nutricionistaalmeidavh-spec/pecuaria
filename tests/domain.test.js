import test from 'node:test';import assert from 'node:assert/strict';import {createAnimal,recordWeight,createCattleTrade} from '../src/index.js';test('animal, pesagem e negócio preservam domínio pecuário',()=>{const a=createAnimal({id:'a1',tag:'001',farmUnitId:'farm-1',lotId:'l1'}),w=recordWeight(a,{weightKg:410,measuredAt:'2026-09-18'}),t=createCattleTrade({id:'t1',type:'sale',partyId:'p1',animalIds:['a1'],totalAmountMinor:500000,occurredAt:'2026-09-18'});assert.equal(w.weights[0].weightKg,410);assert.equal(t.type,'sale')});
