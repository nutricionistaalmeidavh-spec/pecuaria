import {app,BrowserWindow,ipcMain} from 'electron';import {join,dirname} from 'node:path';import {fileURLToPath} from 'node:url';import {createStandaloneHost} from '../runtime/host.mjs';const here=dirname(fileURLToPath(import.meta.url));let host;app.whenReady().then(async()=>{host=await createStandaloneHost({dataDir:join(app.getPath('userData'),'data')});for(const n of ['describe','authState','bootstrap','login','validate','logout','load','action'])ipcMain.handle(`artisys:${n}`,(_e,p)=>host.backend[n](p));const win=new BrowserWindow({width:1440,height:900,minWidth:1024,minHeight:680,show:false,webPreferences:{preload:join(here,'preload.cjs'),contextIsolation:true,nodeIntegration:false,sandbox:true}});await win.loadFile(join(here,'../dist/index.html'));win.once('ready-to-show',()=>win.show())});app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});app.on('before-quit',()=>void host?.close?.());
