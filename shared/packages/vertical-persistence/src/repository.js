const text=(value,label)=>{if(typeof value!=='string'||value.trim()==='')throw new TypeError(`${label} is required.`);return value.trim();};export function createEntityRepository(persistence,{collection,idOf=(entity)=>entity?.id,validate=(entity)=>entity}={}){if(!persistence||typeof persistence.putRecord!=='function')throw new TypeError('Persistence adapter is required.');const name=text(collection,'Collection');return Object.freeze({async save(entity,{expectedVersion}={}){const normalized=validate(entity);const id=text(idOf(normalized),'Entity id');return persistence.putRecord(name,id,normalized,{expectedVersion});},get:(id,options)=>persistence.getRecord(name,text(id,'Entity id'),options),list:(options)=>persistence.listRecords(name,options),remove:(id,options)=>persistence.softDeleteRecord(name,text(id,'Entity id'),options),history:(id)=>persistence.recordHistory(name,text(id,'Entity id'))});}
